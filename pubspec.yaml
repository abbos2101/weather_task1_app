name: weather_task1_app
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bloc_concurrency: ^0.2.0
  cupertino_icons: ^1.0.2
  dio: ^4.0.6
  equatable: ^2.0.3
  flutter_bloc: ^8.0.1
  flutter_native_splash: ^2.1.2+1  # readme pubspec_splash.yaml
  flutter_svg: ^1.0.3
  get_it: ^7.2.0
  google_fonts: ^2.3.1
  hive: ^2.1.0
  path_provider: ^2.0.9
  provider: ^6.0.2
  share_plus: ^4.0.3
  shimmer: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/